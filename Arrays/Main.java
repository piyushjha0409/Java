import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner scn = new Scanner(System.in);

        //storing the 5 roll no:
        // Synatax: dataType[] variable_name = new datatype[size];
        // int[] rnos = new int [5];

        //or directly
        // int[] rno2 = {136,46,1,46};

        // int[] ros; //declaration of the array ros is getting in the stack
        // ros = new int[5];
        // System.out.println(ros[1]);


        //array of primitives:
      //  int[] arr = new int[5];

        //taking the input of the array
        // for (int i = 0; i < arr.length; i++) {
        //     arr[i] = scn.nextInt();
        //
        // }

        //printing the array

//way 1
      //  System.out.println(array.toString(arr));   //This is the best method

//way 2
        // for (int i = 0; i < arr.length; i++) {
        //     System.out.print(arr[i] + " ");
        // }
//way 3
        // for (int num : arr) {
        //   System.out.print(num + " "); //num - elements of the array
        // }

    }
}
